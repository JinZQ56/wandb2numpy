# This is the minimal working config. Because not specified otherwise, all runs in the project
# that include the field "test_acc" will be exported as a numpy array to the directory "wandb_data".

experiment1:
  fields: [ "evaluation_is_success_mean",
            "num_global_steps",
            "evaluation_hit_ball_mean",
            "evaluation_ball_returned_success_mean",
            "evaluation_episode_reward_mean" ]
  entity: gelikit
  project: table_tennis_4d_bbrl


  config:
    sampler.args.env_id:
      values: [ "TableTennis4DProDMP-v0" ]
  #  groups: ["BBRL, new bp reward setting"]
  groups: [ "bbrl ProDMP 20 seeds 300 length ep without truncate after landing 0.69, 0.48" ]
  output_path: /home/lige/Codes/wandb2numpy/wandb_data/table_tennis_bbrl_prodmp
